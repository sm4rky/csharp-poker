import {JSX} from "react";


interface PokerTableProps {

}

// Encapsulates all gameplay features, after having joined a table from the lobby
export function PokerTable(props: PokerTableProps): JSX.Element {
    return <></>;
}